# KaKao Clone

```
this project is yang sun's first project
```

## 기억할것

모든것이 끝났을때 (>)또는 (;)를 쳐준다.

```
태그란 <이름 속성="값">내용</이름>
태크란 (<a href="op.gg"><a>),(<p></p>),(<title><title>)←처럼 브라우저가 알수있는 태그들이 정해져있다.
링크의 경우<a href="op.gg">URL의 이름을 설정할수있다.</a>
<title>탭 마다의 제목을 설정할수있음</title>
문단태그란 <p>내가 하고싶은 텍스트를 설정할수있다.</p>
<!DOCTYPE html>이 태그는 구글 크롬으로 하여금 이 문서가 html문서임을 알려줌 또한 <html></html>로 문서를 여닫을수있음
<html> ( 내용 ) </html>   (<html>안에있는 내용들을 몽땅 html문서라고 할수있음)
<head> 현재 URL위쪽으로 <head>라고 생각하자 그 아래 모든것들이 <Body> 유투브로 태그부터 아래의 끝까지가 Body
Meta의 뜻은 엑스트라,추가정보와 같은뜻이야
Semantic 태그는 내가 말하고자 하는것에 박스를 가두는것이고
Non-Semantic 태그는 내가말하고자 하는 줄에 박스를 가두는것이다 (확실하지않음)
패팅(padding)은 박스의 보더에서 안쪽으로 있는 간격을 뜻해.
마진(margin)은 박스의 보더에서 바깥쪽으로 있는 간격을 뜻해.

```

## 잘 이해 안되는 것

```
-<target="blank> attribute(속성)을 활용해서 html을 작성하는 방법이다.
-span 태그는 non-semantic 태그야. apan태그는 짧다는 뜻인데 그게다야 이게짧은 타이틀인지
 문단인건지 설명인건지 전~혀알수없어
3-3 Mixing CSS with HTML


```
